{
  "mcpServers": {
    "hoplog": {
      "command": "docker",
      "args": [
        "compose",
        "-f", "/home/xi/docker/hoplog/compose.yaml",
        "run", "--rm",
        "-e", "NENE2_LOCAL_API_BASE_URL=http://app:8080",
        "app",
        "php", "bin/mcp-server.php"
      ]
    }
  }
}
